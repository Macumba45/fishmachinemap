export type User = {
    id: number
    role: string
    name: string
    email: string
    password: string
    picture: string
}

export type Experiences = {
    title: string
    category: string
    description: string
    price: string
    phone: string
    url: string
}
