import styled from 'styled-components'

export const ButtonContainer = styled.div`
    display: flex;
    position: absolute;
    top: 1rem;
    align-items: center;
    border-radius: 100px;
    z-index: 999;
`
